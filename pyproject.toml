[project]
name = "atv1-perceptron"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "matplotlib>=3.11.1",
    "numpy>=2.5.3",
]

[dependency-groups]
dev = [
    "notebook>=7.6.2",
]
